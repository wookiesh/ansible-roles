---
# NOTE: docker_stacks is deprecated — replaced by per-application roles.
# This test only verifies the role is syntactically valid.
- name: Test docker_stacks role (deprecated)
  hosts: localhost
  become: true
  gather_facts: true
  vars:
    docker_stacks_enabled: false
  tasks:
    - name: Include docker_stacks role
      ansible.builtin.include_role:
        name: docker_stacks
