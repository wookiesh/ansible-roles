---
- name: Restart Alloy
  ansible.builtin.command:
    cmd: >-
      {{ 'docker compose -f ' + alloy_compose_file + ' restart alloy'
         if alloy_deployment_mode == 'compose'
         else 'docker service update alloy_alloy --force' }}
  changed_when: false
  failed_when: false
