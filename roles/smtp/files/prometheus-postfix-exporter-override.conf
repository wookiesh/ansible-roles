[Service]
ExecStart=
ExecStart=/usr/bin/prometheus-postfix-exporter --postfix.logfile_path=/var/log/mail.log --web.listen-address=127.0.0.1:9154
# /var/log/mail.log is syslog:adm 0640 — add adm so the prometheus user can read it.
SupplementaryGroups=adm
